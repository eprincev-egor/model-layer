"use strict";

const assert = require("assert");
const index = require("../lib/index");

describe("test index", () => {

    it("index has Model", () => {

        assert.ok( index.Model );

    });

});